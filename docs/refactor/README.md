# 리팩터링 백로그

`docs/` 아래 세 번째 축입니다. 옆 두 디렉터리와 역할이 다릅니다.

- [**`../architecture/`**](../architecture/) — **지금 어떻게 동작하는가.** 정상 흐름과 분기를 그림으로 설명합니다.
- [**`../troubleshooting/`**](../troubleshooting/) — **무엇이 잘못됐고 왜 그렇게 고쳤는가.** 겪은 장애와 내린 판단의 기록입니다.
- **여기(`refactor/`)** — **아직 하지 않은 일.** 알고는 있지만 손대지 않은 것, 그리고 **손대지 않기로 결정한 것**입니다.

이 디렉터리가 필요해진 이유가 있습니다. 헥사고날 경계를 되돌리는 작업을 다섯 커밋에 걸쳐 해 왔는데,
매번 "고칠 것을 찾아 그때그때 고치는" 방식이었습니다. 그래서 **무엇이 남았는지가 아무 데도 적혀 있지
않았고**, 남은 항목은 다음 사람이 코드를 읽다가 다시 발견하는 수밖에 없었습니다. 여기에 적어 두면
적어도 두 번 발견하지는 않습니다.

## 항목

| # | 제목 | 한 줄 요약 | 성격 | 상태 |
| --- | --- | --- | --- | --- |
| 01 | [죽은 코드 세 건](01-dead-code.md) | 호출처가 0인 서비스 하나, 참조가 0인 DTO 하나, 빈 패키지 하나 | 정리 | 대기 |
| 02 | [`@Transactional`이 `Mono` 위에서 무효화된다](02-transactional-on-mono.md) | 애노테이션이 **지키지 않는 보장을 약속**한다. 같은 저장소가 이미 정답을 알고 있다 | 동작 | 대기 |
| 03 | [리스너 분리가 남긴 낡은 주석](03-stale-javadoc-after-listener-split.md) | 주석이 트랜잭션 소유자를 이름으로 지목해 두어, 그 클래스가 바뀌자 함께 낡았다 | 문서 | 대기 |
| 04 | [아웃바운드 포트 구현체 3개가 인프라에 있다](04-outbound-adapter-location.md) | 포트 18개 중 15개는 `adapter.out`. 방금 인바운드에서 한 일의 **아웃바운드 짝** | 경계 | 대기 |
| 05 | [웹 관심사가 인프라에 있다](05-web-concerns-in-infrastructure.md) | `@RestControllerAdvice`와 HTTP 응답 봉투가 `infrastructure`에. 04와 같은 성격, 범위만 큼 | 경계 | 대기 |
| 06 | [애플리케이션이 스프링 예외를 잡는다](06-framework-exception-leak.md) | 이름이 같은 예외가 두 벌. **현재 ArchUnit 규칙의 사각지대** | 경계 | 대기 |
| 07 | [OAuth 클라이언트 선택이 세 벌 중복](07-oauth-client-selection-duplication.md) | 같은 `getClient`가 세 곳에. 선택 자체를 없앨 수 있다 | 중복 | 대기 |
| 08 | [유스케이스를 여럿 겸하는 서비스 둘](08-fat-service-runningart.md) | `RunningArtService`가 5개, `AiGenerationService`가 2개. 성격이 서로 다르다 | 응집도 | 대기 |
| 09 | [`TokenPersistencePort`가 두 책임을 쥔다](09-fat-port-token-persistence.md) | 세션 관리 10개 + 블랙리스트 4개. 구현체는 저장소 최대 클래스 | 응집도 | 대기 |
| 10 | [포트가 `Mono`·`Flux`를 노출한다](10-reactive-types-in-ports.md) | **고치지 않기로 한 항목.** 리액티브 타입은 문제가 아니다 — 02와 함께 읽어야 한다 | 판단 기록 | 결정 |
| 11 | [영속성 어댑터 테스트 공백](11-test-gaps-persistence-adapters.md) | 365줄·206줄 어댑터에 단위 테스트가 없다. 패턴은 이미 옆에 있다 | 테스트 | 대기 |

## 권장 처리 순서와 그 이유

번호는 사건 순서가 아니라 **처리 순서**입니다. 앞을 닫으면 뒤가 줄어드는 의존이 실제로 있습니다.

- **01을 먼저** — `SocialTokenService`를 지우면 02의 대상이 둘에서 하나로, 07의 중복이 셋에서 둘로 줄어듭니다.
  가장 쉬운 항목이 다른 두 항목을 깎아 줍니다.
- **02·03은 그다음** — 02는 **유일하게 동작이 틀린 항목**이고, 03은 직전 커밋이 남긴 것이라 지금 안 하면
  영영 안 합니다. 둘 다 작습니다.
- **04 → 05 순서로** — 같은 성격인데 05가 훨씬 넓습니다(컨트롤러 6개가 딸려 옵니다). 좁은 쪽에서
  패키지 모양과 ArchUnit 규칙을 먼저 확정하고 넓은 쪽으로 갑니다.
- **11이 09보다 먼저** — `TokenPersistenceAdapter`는 365줄에 테스트가 없습니다. **테스트 없이 나누면
  나눈 것이 맞는지 알 방법이 없습니다.** 그물을 먼저 치고 자릅니다.
- **08·10은 언제든** — 다른 항목과 얽히지 않습니다. 10은 이미 결정이 끝나 읽기만 하면 됩니다.

## 관통하는 규칙 몇 가지

옆 디렉터리의 같은 절과 호응합니다. 조사에서 **실제로 여러 항목에 걸쳐 반복 관찰된 것**만 적습니다.

- **애노테이션이 지키지 못하는 보장을 약속하게 두지 않는다** — 무효한 `@Transactional`은 없느니만
  못합니다. 아무 일도 안 하면서 "여기는 안전하다"고 말하기 때문입니다. (02)
- **포트를 구현하면 어댑터다** — 트리거가 무엇인지도, 어떤 기술을 쓰는지도 자리를 정하지 않습니다.
  **방향이 정합니다.** 인바운드에서 이미 한 번 확인한 규칙이고, 아웃바운드에도 그대로 적용됩니다. (04, 05)
- **경계는 규약이 아니라 테스트가 지킨다** — 그리고 **규칙이 덮지 않는 경계는 반드시 샙니다.**
  06번이 지금까지 살아남은 이유는 규칙이 패키지 이름만 봤기 때문입니다. (04, 05, 06)
- **설명이 코드보다 오래 산다** — 주석이 다른 클래스를 이름으로 지목하면, 그 클래스가 움직이는 날
  주석은 조용히 거짓말이 됩니다. (03, 08)
- **테스트를 먼저 씌우고 자른다** — 리팩터링이 동작을 바꾸지 않았다는 것을 무엇이 말해 주는지가
  먼저입니다. (09, 11)

## 문서 규약

`../troubleshooting/README.md`와 같은 규약을 씁니다.

- 파일명은 `NN-영문-kebab-case.md`. 번호는 **권장 처리 순서**입니다.
- 절 구성: **무엇이 문제인가 → 근거 → 왜 고쳐야 하는가 → 어떻게 → 재발 방지 → 하지 않기로 한 것.**
  쓸 내용이 없는 절은 생략합니다.
- 문서 머리에 **상태 한 줄**을 둡니다. 항목을 닫을 때 `완료 · 커밋 <해시>`로 바꾸고, 이 README의
  표도 함께 갱신합니다.
- 각 문서의 **근거는 다시 확인할 수 있는 명령과 함께** 적습니다. 조사 결과를 문장으로만 남기면
  코드가 움직였을 때 그것이 아직 사실인지 알 수 없습니다.
- 코드 인용은 5~15줄로 자르고 전체는 파일 링크로 넘깁니다.
- **라인 번호를 본문에 박지 않습니다.** 파일명과 메서드명으로 가리킵니다.

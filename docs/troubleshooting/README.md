# 트러블슈팅 상세 기록

[`../../README.md`](../../README.md)의 **기술적 의사결정 및 트러블슈팅** 섹션을 항목별로 확장한 문서들입니다.

README는 훑는 문서입니다. 여기는 **파고드는 문서**로, README에는 자리가 없던 네 가지를 담습니다.

- 문제가 된 코드와 고친 코드의 **실제 인용**
- **검토했다 기각한 대안**과 그 이유
- **재현·검증 방법** (테스트, `EXPLAIN`, `curl`, Redis 명령)
- 순서가 핵심인 흐름의 **다이어그램**

여기는 **무엇이 잘못됐고 왜 그렇게 고쳤는가**를 다룹니다. 장애가 아니라 **지금 어떻게 동작하는가**를
찾고 있다면 [`../architecture/`](../architecture/), 아직 **고치지 않은 것과 고치지 않기로 한 것**을
찾고 있다면 [`../refactor/`](../refactor/) 쪽입니다.

| # | 제목 | 한 줄 요약 |
| --- | --- | --- |
| 01 | [리액티브 파이프라인의 블로킹 I/O](01-webflux-blocking-io.md) | 이벤트 루프에서 JDBC를 기다리면 **모든 아웃바운드 외부 API 호출**이 함께 멈춘다 |
| 02 | [S3 고아 파일 — Saga 보상 트랜잭션](02-s3-orphan-saga-compensation.md) | 실패 지점이 둘이고, 보상은 **원본 예외를 가리지 않아야** 한다 |
| 03 | [SQS 콜백 경합 조건](03-sqs-callback-race-condition.md) | 비관적 락으로 직렬화하고, `PENDING`이면 예외로 **SQS 재시도를 유도**한다 |
| 04 | [스케줄러 중복 실행 — ShedLock](04-scheduler-duplicate-shedlock.md) | `lockAtLeastFor`는 시계 오차를, `lockAtMostFor`는 데드락을 막는다 (방향이 반대) |
| 05 | [Rate Limiting — Lua 토큰 버킷](05-rate-limit-lua-token-bucket.md) | `GET→계산→SET`의 창을 Lua로 닫고, 폴백은 USER·IP를 **비대칭**으로 다룬다 |
| 06 | [Refresh Token Rotation](06-refresh-token-rotation.md) | 네 연산을 한 스크립트로 묶고, 블랙리스트 TTL은 구 토큰의 **잔여 수명**을 쓴다 |
| 07 | [소셜 계정 중복 가입](07-oauth-duplicate-account.md) | `columnNames`가 필드명을 가리켜 **제약이 만들어진 적이 없었다** |
| 08 | [스케줄러의 숨은 비용](08-scheduler-full-scan-and-write-amplification.md) | 5분마다 풀 스캔 + 문장 2N개. 인덱스 **컬럼 순서**와 벌크 UPDATE의 3대 함정 |
| 09 | [Flyway·Hibernate 스키마 드리프트](09-schema-drift-flyway-hibernate.md) | MySQL ENUM은 순번으로 저장된다. `ALGORITHM=COPY`가 데이터를 지켰다 |
| 10 | [배포 전에 잡은 기동 실패](10-sqs-listener-startup-failure.md) | 자격증명 없이는 `@SqsListener`가 컨텍스트 기동을 중단시킨다 |
| 11 | [설계 기록](11-design-notes.md) | 하나의 장애로 떨어지지는 않지만 남겨 둘 판단들 |
| 12 | [메시지 브로커 선택 — 왜 Kafka가 아닌가](12-why-not-kafka.md) | 발행자가 SNS라 브로커 교체는 브릿지 추가다. **replay는 브로커가 아니라 원본 보존이 결정한다** |

## 관통하는 규칙 몇 가지

여러 문서에 반복해서 나타나는 판단들입니다.

- **외부 시스템에 대한 되돌릴 수 없는 행위는 커밋 이후에** — S3 삭제, SSE 알림, Redis GEO 갱신이
  모두 `afterCommit`에 있습니다. (03, 08, 11)
- **트랜잭션은 넓게 걸수록 안전하지 않다** — 바깥 트랜잭션이 제약 위반을 커밋 시점으로 미루면
  그 위반을 잡아 처리하는 코드가 실행되지 않습니다. (01, 07, 11)
- **정합성의 최종 근거는 DB 제약이다** — 애플리케이션의 "조회 후 없으면 삽입"은 중복을 줄일 뿐
  거부하지 못합니다. (07, 11)
- **감지가 아니라 직렬화가 필요할 때가 있다** — 낙관적 락은 충돌을 알려 줄 뿐 기다려 주지 않습니다. (03)
- **끄는 쪽을 명시적 선택으로** — 기능 스위치의 기본값은 운영 동작이어야 합니다. (10)
- **이식성은 인터페이스가 아니라 의미가 결정한다** — 가시성 타임아웃에 기댄 재시도는 포트를 뚫어도
  옮겨지지 않습니다. 미리 만든 추상화가 감추지 못하는 것이 있습니다. (03, 12)

## 문서 규약

- 파일명은 `NN-영문-kebab-case.md`. 한글 파일명은 URL 인코딩되어 링크가 읽기 어려워집니다.
- 절 구성은 **상황 → 증상 → 원인 → 검토한 대안 → 해결 → 검증 → 남은 과제**. 쓸 내용이 없는 절은 생략합니다.
- 코드 인용은 5~15줄로 자르고 전체는 파일 링크로 넘깁니다. 문서가 코드 사본이 되면 금방 낡습니다.
- **라인 번호를 본문에 박지 않습니다.** 파일명과 메서드명으로 가리켜, 코드가 움직여도 문서가 틀리지 않게 합니다.
- 새 트러블슈팅을 추가할 때는 **README 요약과 이 디렉터리의 상세 문서를 함께** 갱신합니다.
